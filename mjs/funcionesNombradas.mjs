//Exportacion directa de funciones nombradas
export const sumar = (num1, num2) => {
    return num1 + num2;
}

//Exportacion directa de funciones nombradas
export const restar = (num1, num2) => {
    return num1 - num2;
}